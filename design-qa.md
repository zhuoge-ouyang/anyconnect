# 方案 3 · Windows 管理台视觉与交互验收

日期：2026-09-22。使用 product-design:image-to-code / design-qa，将用户已选方案落实到现有 Windows WinForms 管理台，而不是建立新的网页项目。没有修改 VPN 连接、认证或分流协议。

## 视觉真值与证据

- 真值：`C:/Users/MSI/.codex/generated_images/01a03a3c-cb38-7313-ad7d-bf051da4b5fa/exec-300ee854-7a8f-45d6-a461-8e2f7108f685.png`，1488×1058。
- 实现：`artifacts/ui-option3/connection.png`，1008×716。
- 全图同一输入对比：`artifacts/ui-option3/comparison.png`，左参考、右实现。
- 关键内容局部对比：`artifacts/ui-option3/comparison-focus.png`，连接标题、主操作、模式与网址输入。
- 其他实窗状态：`settings.png`、`validation-error.png`、`disconnected.png`、`narrow.png`、`state-unavailable.png`，均位于上述证据目录。
- 参考按相同比例归一至 1008×716；局部对比裁取两图相同区域 (274,80,710,520)。本项目为原生 Windows 控件，无 CSS viewport/deviceScaleFactor；截图为原生客户区像素，正常视口 1008×716，小窗口视口 900×716。
- 状态：隔离测试快照中的“已连接 / 深圳 / 国内直连”。不是对真实 VPN 当前联网能力的证明。
- 最终截图通过 Win32 PrintWindow 捕获本次测试创建的真实窗口客户区，不抓取用户其他窗口；保留操作系统标题栏，不将生成图里的窗口按钮另画一遍。

## 五项必查

1. 字体：沿用系统 Microsoft YaHei UI，主标题、线路、区块标题与正文层级清楚；系统控件和字体光栅化与生成图略有差异，未引入需额外安装的字体。
2. 布局：24% 插画侧栏、右侧三段主内容、单一连接动作、固定底部日志入口。输入与按钮高度、标题位置和底部留白已对齐。小窗口下品牌名不截断，操作区无水平溢出。
3. 色彩：米白背景、深绿文字、青蓝操作色，错误／不可用状态有文字反馈。未添加伪造的安全认证标识。
4. 图像：内置默认绘图工具基于所选图生成独立森林石桥背景，源尺寸 724×2172，按比例 cover 绘制，不变形；品牌图标复用项目原有 PNG，不使用绘图代码替代图片。用户已授权内置默认模型，实际模型名未暴露。
5. 文案：按真实配置语义纠正生成图。“国内直连”默认直连，仅国外白名单走 VPN；“VPN 优先”保留国内白名单例外，不称为无例外的“全局代理”。提交提示不冒称已经保存。连接状态不可读时禁用操作，不继续显示已连接。

## 对比历史与修复

- 初次捕获：品牌 ICO 转 Bitmap 出现噪点 [P1]，导航文字偏小 [P2]，小窗口品牌名截断 [P2]。初次证据保留为 `connection-before.png`、`settings-before.png`。
- 已修复：品牌图片改用项目原始 PNG；导航和添加按钮字号上调；窄侧栏字体适配。
- 第二次对比：原生单行输入框自动高度小于参考 [P2]，主标题及底部纵向位置偏移 [P2]。改为有内边距的原生输入容器并调整坐标。
- 最终捕获：改用原生 PrintWindow，避免 DrawToBitmap 不遵守圆角 Region 及空白区合成造成的误判。重新生成全图／局部对比并检查上述五项；未发现阻塞交付的 P0/P1/P2。

## 交互验证

- 真正点击 WinForms 按钮／导航，验证设置页切换与返回、日志、连接／断开、分流模式命令。
- URL 自动移除路径、查询和端口并归一化域名；IPv4 转 /32；IPv6 CIDR 转网络地址。
- 无效输入、非 HTTP(S)、带账号密码的网址、非法 CIDR、空输入、重复域名不发送添加命令。
- 生成的 7 条 JSON 命令通过 Go `Command.Validate` 与精确 action/value 断言。刷新控件不反向发送命令。
- 命令先写临时文件，再原子重命名，避免后台读取半份 JSON。
- 连接、断开、输入错误、小窗口及状态损坏状态均捕获；测试不启动 VPN、不修改实际路由。
- `go test ./... -count=1` 通过；最终视觉与命令写入调整后重跑 `go test ./internal/ui -count=1` 通过。
- 发布支持 27 项检查通过。1.0.2.0 完整包实际解包后 33 文件 SHA-256 与清单一致。

## 预期差异／剩余限制

- 原生单选按钮、输入边框、字体渲染及已有石桥品牌图标与生成图不完全相同，属于保留现有桌面产品和 Windows 控件行为的适配；不进行逐像素仿造。
- 顶部“分流规则”打开现有可操作规则窗口；设置中保留智能选线、更新 IP 库、开机启动、诊断、充值说明和联系作者，未删减功能。
- 本轮是已选管理台界面的落地。登录／安装器没有重画，仍使用现有 PowerShell WinForms 宿主；原生宿主迁移、标准卸载交互不在本轮完成范围。
- 当前没有代码签名证书，完整包仍 NotSigned；界面改版和测试通过都不能证明杀毒误报已经消除。未执行真实升级安装或重新联网验收。
- 多显示器不同 DPI 的人工验收仍待进行。系统控件圆角、字重及装饰性状态图标可作为后续 P3 微调，不影响当前功能。

final result: passed
# 原生宿主迁移验收补充 · 2026-09-22

本轮不改变已批准的方案 3 设计，迁移为 C# WinForms。已检查真实控件渲染：

- [管理台](artifacts/native-ui-20260922/022409.551063400/dashboard.png)
- [设置与诊断](artifacts/native-ui-20260922/022409.551063400/settings.png)
- [登录](artifacts/native-ui-20260922/022409.551063400/login.png)
- [白名单](artifacts/native-ui-20260922/022409.551063400/whitelist.png)
- [安装向导](artifacts/native-ui-20260922/022409.551063400/installer.png)

森林石桥侧栏、连接状态、两种模式、统一输入、设置与诊断结构保留。48 项原生控件检查通过，包括原站点下拉选择保护、节点名称中的分隔符、网址/IP/CIDR、错误/重复输入、模式切换、智能选线互锁和采用/恢复。截图由真实 WinForms 窗口渲染导出，使用隔离测试状态；不是桌面实时联网截图。当前机器尺寸已检查，其他 DPI/多屏环境尚未实机验收。

## 最新验收：网速与首页选站点 · 1.0.5.0 · 2026-09-22

### 目标、视口与证据

用户选择**本轮第一个显示的生成结果**，不是最早提交的请求：`exec-b13e4d07-a5ec-449b-826c-408675bcb721.png`，即右侧独立监测栏。视觉真值已保存为 [参考图](artifacts/traffic-ui-20260922/selected-reference.png)。复用现有森林石桥侧栏图片、品牌图标和 Microsoft YaHei UI 字体；没有初始化网页项目、添加 WebView 或重画品牌素材。

- 源图 1488×1058；真实 WinForms 客户区 1008×716，按相同比例归一源图，去除系统窗口框差异。Windows 当前测试显示缩放 100%；不是 CSS viewport。大窗口仅验证可用性，不将其误记为不同系统 DPI。
- 状态：连接正常、深圳、国内直连；采样曲线使用明确标注的隔离示例。采样器另做真实网卡只读验证，不把示例图当作当前 VPN 实际速度。
- [最终管理台](artifacts/traffic-ui-20260922/181141.214476900/dashboard.png)、[全图同输入对比](artifacts/traffic-ui-20260922/181141.214476900/comparison.png)、[网速局部同输入对比](artifacts/traffic-ui-20260922/181141.214476900/comparison-traffic.png)。先合并为同一张对照输入，再检查，不以分开查看冒充并排比较。
- [900×740 窄窗口](artifacts/traffic-ui-20260922/181141.214476900/narrow.png)、[1488×1058 大窗口](artifacts/traffic-ui-20260922/181141.214476900/large.png)、[设置](artifacts/traffic-ui-20260922/181141.214476900/settings.png)。窄/大窗口亦覆盖不可用/忙碌状态。
- 截图通过 PrintWindow 读取测试创建的窗口客户区，不抓取用户其他应用。

### 修复历史

1. `180751.207888000` 首轮：[P2] 默认窗口额外滚动条影响内容密度、窄侧栏品牌截断、网速图缺时间端点、标题过靠上。修复最小内容高度、窄侧栏字号、图表端点与垂直布局。
2. `180932.898151300` 第二轮：[P2] 时间标签被 Chart 遮挡、窄侧栏副标题与品牌图标重叠。提升标签层级，将窄窗口副标题放到图标下方；数值与单位分别排版，避免长单位占满右栏。
3. `181141.214476900` 最终轮：重新捕获并检查全图/局部对比，以上 P2 已修正；默认窗口主要内容无多余滚动条，站点与添加按钮不越界。

### 五项视觉检查

- 字体：系统中文字体、深绿状态标题、较大速率数字和较小单位保持层级；原生控件字号/字重与生成图存在轻微差异，不把 Windows 字体栅格化视为逐像素一致。
- 布局：24% 插画栏、约 45% 操作区、31% 网速栏；上传/下载各有曲线，站点入口在连接动作前，底部日志可用。设置页隐藏右栏并恢复完整工作宽度。窄窗副标题重新排版，大窗保留原生字号和额外留白。
- 颜色：米白主体、浅绿网速栏、青色下载曲线、蓝色上传曲线；保留有文字的不可用状态。数字之外还显示网卡名称及统计范围。
- 图片与图标：真实项目插画及原品牌图标按比例绘制；生成稿不同的徽标不替换现有品牌。示意性的箭头圆标、定位和 Wi-Fi 装饰图标未加入，用清晰标签/原生下拉箭头承担含义，不用自制图形或字符伪装素材。
- 文案：纠正参考图的“全局代理”为真实的“VPN 优先”；国内直连默认直连，仅国外白名单走 VPN。速率采用 B/s 系列而非草图 Mbps，不显示虚构 LAN IP；采集不可用为破折号和原因，不是 0。生产不包含示例曲线。

### 行为、验证与边界

69 项真实 WinForms 控件/采样检查通过，包括：站点名称中分隔符、刷新保留待选站点、取消不发命令、同站点不切换、重复/忙碌互锁、恢复选择、网址/IP 添加、导航和旧功能。采样覆盖实际时间差、零流量、首样本、计数器重置、接口改变、休眠间隔、60 秒有界历史、读取异常和真实网卡累计计数。

Go 测试覆盖可信站点解析、无效站点/缺凭据在断开前拒绝、断开和连接顺序、任一失败停止且不自动换节点、命令及快照往返；C# 实际输出再经 Go 协议校验。全量测试、相关 vet、27 项发布检查通过；最终完整包 35 个文件哈希匹配，包内宿主实际就绪/加载图表/关闭通过。

未改变当前 VPN 或本机安装；真实确认切换后跨站点联网、新机安装、不同 DPI/多显示器仍未实机验收。模拟确认委托验证取消/确认逻辑，不将其称为真实线路切换。

### 结论与后续微调

无剩余阻塞性 P0/P1/P2。P3：原生下拉框边框、单选控件和字体与生成图有系统样式差异；装饰图标不影响功能，未新增图标依赖。保留已批准的视觉结构和真实 Windows 控件，不宣称逐像素复制。

final result: passed

---
