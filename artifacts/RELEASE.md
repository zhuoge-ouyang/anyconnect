# Windows 完整安装包 · 1.0.4.0 · 2026-09-22

当前文件：`AnyConnectSplitTunnelSetup.exe`（72,605,184 字节，约 69.24 MiB），完整安装包，不是补丁。内置文件共 35 个，含版本一致的 `anyconnect-ui.exe` 原生桌面宿主。

SHA-256：`9133B0BD15608A575512E435777879ACE31A915D96EB8CDACDAE7FD2E654321C`。

## 本版变更

- 登录、方案 3 管理台、白名单、智能选线推荐、联系作者、充值说明和安装向导由编译后的 C# WinForms 宿主运行，不再执行界面 PowerShell 脚本。
- Go 继续负责连接、路由和配置，状态/命令协议不变。登录凭据通过进程管道返回，不再写临时 JSON 或放在命令行中。
- 原生宿主就绪握手、单窗口恢复与聚焦、父进程退出清理；启动失败明确报错，无脚本界面 fallback。
- 宿主进入统一版本、签名流程及标准卸载清单。安装向导检查 .NET Framework 4.8 或更高版本。

## 本版验证

全量 `go test ./... -count=1` 通过，最终安装器改动另有包测试通过；相关 `go vet` 与 diff 检查通过。48 项真实原生 WinForms 控件检查、Go/C# 命令兼容性、实际登录窗口管道（虚构凭据、中文内容及取消）、管理台启动/恢复/关闭及父进程退出检查通过。发布流程 27 项检查通过。

最终安装包隔离解包后 35 个文件逐项通过 SHA-256 核验；原生宿主确实在编译内置卸载清单中，真实解包文件的两种配置保留模式卸载回归通过。发布配置与 `configs/config.dist.yaml` 哈希一致。

实际启动最终包中的原生安装向导并关闭，已验证临时宿主自动清理；未点击安装、未修改现有安装位置。渲染证据见 `native-ui-20260922/022409.551063400/`，使用隔离状态，不代表真实 VPN 联网截图。

## 升级与限制

先断开 VPN、从托盘退出旧程序并关闭管理台，再运行此完整包覆盖安装。只复制 Go 主程序会缺少新宿主，不能代替完整安装。当前正在运行的 `bin/anyconnect-split.exe` 未覆盖、未停止，VPN 未重启。

当前包仍为 **NotSigned**，发布者未填写，不能保证消除误报。系统辅助操作仍有 PowerShell 调用，不是项目全面去脚本；联网协议和权限未改。不同 DPI/多屏、新机联网及完整真实安装-卸载-重启周期仍需实机验收。

上一版完整包和说明备份：`backups/native-host-20260922/release-1.0.3/`；迁移前 UI 和安装源文件备份：`backups/native-host-20260922/`，其中 Go 源码备份以 `.go.bak` 保存，防止进入全量测试。个人配置与安卓产物未改动。

实现和边界见 `../docs/native-ui-host.md`。以下为历史记录，历史大小、哈希及“未完成”描述不适用于当前版本。

---

## 历史记录：1.0.3.0（标准卸载版）

当前分发文件：`AnyConnectSplitTunnelSetup.exe`（72,177,152 字节，约 68.83 MiB），是完整安装包，不是补丁。相邻 `.sha256` 文件用于核对完整性，`payload-manifest.json` 记录 34 个内置文件。

SHA-256：`C46A768226C16B3E4F3777BAAF2B81FBA4EAB0EC88EB00AB1DFBA9031357ADD5`。

保留全部现有功能、网址/IP 白名单修复、方案 3 管理台及内置连接组件，新增独立 `uninstall.exe` 和 Windows 标准卸载入口。

## 使用

先断开 VPN，从托盘退出旧版并关闭管理台，再运行完整安装包覆盖安装。安装后可从 Windows“设置 → 应用 → 已安装的应用”或“控制面板 → 程序和功能”卸载“分流守卫”。直接复制主程序不能补上 Windows 安装记录。

卸载默认保留配置、白名单、日志，可选择删除已知配置及运行文件。未知文件、共享密码凭据、其他安装副本的快捷方式和自启项、独立 Cisco/OpenConnect 与系统驱动均保留。自有组件仍在运行或发现未清理/损坏的路由记录时停止，不强杀连接、不改路由。

卸载程序自身改为唯一临时文件名，在下次重启后由 Windows 清理，不会自动重启，也不会误删重装后的新版卸载程序。根目录可能保留为空目录。凭据管理器中的 `AnyConnectSplitTunnel` 如不再需要，须由用户自行删除。

## 本版验证与限制

- `go test ./... -count=1` 全量通过；最终卸载实现的相关包测试再次通过。
- `go vet ./cmd/uninstaller ./internal/installation ./cmd/installer` 和 diff 空白检查通过。
- 发布流程 27 项检查通过；最终完整包隔离解包后，34 个文件逐项 SHA-256 匹配。
- 从最终卸载二进制读取编译内置的 33 文件删除清单，集合与 payload（除卸载程序自身）一致；用此清单和最终真实解包文件完成两种配置保留模式的隔离卸载回归。
- 覆盖路径越界、目录联接、真实测试进程占用、残留/损坏路由、同名不同目标快捷方式和自启项、失败重试；注册表测试使用专属 HKCU 临时键。
- 已验证正在运行的隔离测试 EXE 可以改为唯一名称及恢复，不写真实系统的重启删除队列。
- 发布配置哈希与 `configs/config.dist.yaml` 相同；未捆绑本机账号、日志和路由恢复文件。

未执行当前真实安装的卸载、UAC 交互、系统应用列表点击卸载或重启后清理验收；这些仍需在测试电脑/虚拟机验证。没有停止当前 VPN，也没有覆盖正在运行的 `bin/anyconnect-split.exe`。本轮更新的是完整安装包，不代表已升级本机运行版本。

当前仍为 **NotSigned**，没有真实代码签名证书，发布者留空。标准卸载并不保证消除杀毒软件误报；签名、原生宿主迁移及真实新机联网验收仍未完成。

上一版完整包、校验值、发布说明和清单保存在 `backups/standard-uninstall-20260922/`；此前主程序回滚备份继续保留。个人配置未改动，安卓产物未改动。源码及行为说明见 `../docs/packaging.md`。

---

## 历史记录：1.0.2.0（以下哈希和完成状态仅属于上一版）

分发文件：`AnyConnectSplitTunnelSetup.exe`（69,170,688 字节，约 65.97 MiB）。旁边的 `.sha256` 文件用于核对完整性，`payload-manifest.json` 记录内置文件清单。

SHA-256：`F883F3256E1BE2B47AC01BF10986964015DB61F0D8FA823C780B49BEB77C947C`。

从当前工作区最新源码构建，包含现有未提交功能、白名单网址/IP 修复、发布流程加固及用户选定的方案 3 管理台。主程序、OpenConnect、sing-box 1.12.17、Wintun、国内 IP 库、默认配置和界面资源均内置，可复制到其他 Windows x64 电脑安装；不要求另装 Cisco 客户端，未包含 Cisco 官方 MSI。

管理台保留森林石桥侧栏，右侧提供连接状态、真实语义的国内直连／VPN 优先选择、统一网址/IP 输入及日志入口；智能选线、IP 库更新、开机启动、诊断、充值说明、联系作者移至设置，白名单管理保持可用。

验证结果：

- `go test ./... -count=1` 全部通过，包括真实 WinForms 网址/IP 添加回归。
- 最终界面调整后 `go test ./internal/ui -count=1` 通过，覆盖 URL、IPv4、IPv6/CIDR、无效与重复输入、导航、模式、命令和多种状态的真实 WinForms 控件交互；脚本文件启动及退出清理另有集成测试通过。
- 发布流程 27 项检查通过。使用新包 `--install-payload` 隔离解包，33 个文件逐项通过 SHA-256 清单比对。
- 视觉验收见 `../design-qa.md`，真实窗口客户区截图保存在 `ui-option3/`。截图使用隔离状态，未对当前网络发送操作。
- 发布配置与 `configs/config.dist.yaml` 一致，账号为空、自动连接关闭，没有包含本机运行日志或路由状态。
- 未运行安装向导、覆盖本机现有安装或建立新的 VPN 会话；当前连接保持运行。新机器的实际登录与联网仍需安装后验证。

当前包仍为 **NotSigned**，发布者未填写：没有真实代码签名证书。界面改版不等于已经消除病毒／潜在垃圾软件检测；原生宿主迁移、标准卸载改造和真实升级／联网验收未在本轮完成。

已核验并更新 `../bin/anyconnect-split.exe`，SHA-256 为 `44C6D0D4E5C686D8F021A0FEC30C3CB3368D8351110A073156E028CA57549721`。桌面“分流守卫”快捷方式指向该文件。当前旧进程 `bin/anyconnect-split-whitelist-url-fix.exe` 保持运行；退出旧版后再启动快捷方式才会进入新版。

回滚备份：`backups/option3-20260922-013501/`，包含原 canonical 主程序、上一版完整包／校验值／发布说明及原桌面快捷方式。个人配置未覆盖。

此前 56 个历史/重复产物已移入回收站，明细见 `cleanup-20260921.json`；本轮验证用的临时解包及重复构建产物在验证后同样移入回收站，保留最新完整包与一份上述回滚备份。

安卓最新版保留在 `../dist/AnyConnect-Mobile.apk`；`AnyConnectRouteProbe.apk`、`AnyConnectRouteProbeSender.apk` 是用途不同的现行测试工具，保留。安卓本次未重建。
